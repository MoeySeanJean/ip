package jaiden.command;

/**
 * Enum class for all possible command types.
 */
public enum CommandType {
    LISTCOMMAND,
    ADDCOMMAND,
    DELETECOMMAND,
    CHANGEMARKCOMMAND,
    EXITCOMMAND,
    UNKNOWNCOMMAND,
    NULLCOMMAND
}
